var sinon = require('sinon'),
    assert = require('assert');

describe('The Session (Auth) Controller', function () {

  it('should return a 404 when getting nothing', function() {
    assert.ok(true);
  });

});
